import PageShell from "../../components/common/PageShell";

export default function OrderLookupPage() {
  return (
    <PageShell>
      <main className="mx-auto max-w-[900px] px-4 py-10">
        <section className="rounded-[32px] border border-slate-200 bg-white p-8 shadow-sm">
          <div className="text-xs font-black uppercase tracking-[0.25em] text-blue-700">Order Tracking</div>
          <h1 className="mt-3 text-4xl font-black text-slate-950">Tra cứu đơn hàng</h1>
          <p className="mt-3 text-sm font-semibold text-slate-600">Nhập mã đơn hoặc số điện thoại để kiểm tra trạng thái đơn hàng.</p>

          <div className="mt-6 flex gap-3">
            <input className="min-w-0 flex-1 rounded-2xl border border-slate-200 px-4 py-4 text-sm font-semibold outline-none" placeholder="Nhập mã đơn / số điện thoại..." />
            <button className="rounded-2xl bg-blue-700 px-6 py-4 text-sm font-black text-white">Kiểm tra</button>
          </div>
        </section>
      </main>
    </PageShell>
  );
}
