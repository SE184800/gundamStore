export default function AutoTranslate() {
  // Production-safe default: legacy DOM auto-translation is disabled.
  // Pages/components should use explicit i18n copy instead of mutating document.body.
  return null;
}
