PORT=4800
NODE_ENV=development

# PostgreSQL
# Local example:
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/gundam_store?schema=public"
DIRECT_URL="postgresql://postgres:postgres@localhost:5432/gundam_store?schema=public"

# Production example:
# DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/DATABASE?sslmode=require&schema=public"
# DIRECT_URL="postgresql://USER:PASSWORD@HOST:5432/DATABASE?sslmode=require&schema=public"

# JWT
# Production must be at least 32 characters and stored as platform secret.
JWT_SECRET="local-dev-secret-change-in-production"
JWT_EXPIRES_IN="1d"

# CORS
FRONTEND_ORIGIN="http://localhost:5173"
EMAIL_APP_PASSWORD=qdvgbteyinbkihvq
EMAIL_USER= nguyennamminhtri@gmail.com
EMAIL_HOST=smtp.gmail.com
# Optional comma-separated extra origins for UAT/staging.
# CORS_ORIGINS="https://uat.your-domain.com,https://preview.your-domain.com"
